export default class GeneratorIterator<T> implements Iterator<T> {
    private readonly nextFunction;
    constructor(nextFunction: () => T | null | undefined);
    next(value?: any): IteratorResult<T>;
}

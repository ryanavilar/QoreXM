import Sequence from "./Sequence";
export declare class Reverse {
    /**
     * Returns a new sequence with all elements of the sequence in reverse order.
     *
     * @returns {Sequence<T>}
     */
    reverse<T>(this: Sequence<T>): Sequence<T>;
}

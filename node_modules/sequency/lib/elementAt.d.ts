import Sequence from "./Sequence";
export declare class ElementAt {
    /**
     * Returns the element at position `index` (zero-based) or throws an error if `index`
     * is out of bounds.
     *
     * @param {number} index
     * @returns {T}
     */
    elementAt<T>(this: Sequence<T>, index: number): T;
}

import Sequence from "./Sequence";
export declare class ElementAtOrNull {
    /**
     * Returns the element at position `index` (zero-based) or `null` if `index`
     * is out of bounds.
     *
     * @param {number} index
     * @returns {T}
     */
    elementAtOrNull<T>(this: Sequence<T>, index: number): T | null;
}

import { ViewDriver } from "./ViewDriver";
import { CacheRef, ViewDriverObject, QoreRow } from "./Qore";
export declare class NormalizedCache {
    data: Record<string, Record<string, boolean | number | string | CacheRef>>;
    modify(modifier: (data: NormalizedCache["data"]) => void): void;
    identify(table: string, id: string): string;
    lookup(ref: CacheRef, depth?: number): Record<string, any>;
    read<V extends ViewDriver>(view: V, id: string): ViewDriverObject<V>;
    write<V extends ViewDriver, T extends QoreRow>(view: V, rows: T[]): void;
}

import { Command, flags } from "@oclif/command";
export default class CreateProject extends Command {
    static description: string;
    static examples: string[];
    static templatesLocation: string;
    static flags: {
        token: flags.IOptionFlag<string>;
        org: flags.IOptionFlag<string>;
        template: flags.IOptionFlag<string>;
    };
    static args: {
        name: string;
    }[];
    static getTemplates(): string[];
    run(): Promise<void>;
}

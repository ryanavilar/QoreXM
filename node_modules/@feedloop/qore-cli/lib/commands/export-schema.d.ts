import { Command } from "@oclif/command";
import { QoreProjectSchema } from "@feedloop/qore-sdk/lib/project/index";
import { CLIConfig } from "../config";
export default class ExportSchema extends Command {
    static description: string;
    static examples: string[];
    static flags: {
        token: import("@oclif/command/lib/flags").IFlag<string>;
        project: import("@oclif/command/lib/flags").IFlag<string>;
        org: import("@oclif/command/lib/flags").IFlag<string>;
    };
    static args: {
        name: string;
    }[];
    static getSchema(configs: CLIConfig): Promise<QoreProjectSchema>;
    run(): Promise<void>;
}

import Conf, { Schema } from "conf";
export declare type CLIConfig = {
    token: string;
    project: string;
    org: string;
};
export declare const schema: Schema<CLIConfig>;
declare const config: Conf<CLIConfig>;
export default config;

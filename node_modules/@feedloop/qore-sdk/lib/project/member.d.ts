import { ProjectConfig } from "./project";
export declare type APIMember = {
    id: string;
    email: string;
};
export declare type Member = APIMember & {
    _config: ProjectConfig;
    delete(): Promise<void>;
};
export declare class MemberImpl implements Member {
    id: string;
    email: string;
    _config: ProjectConfig;
    constructor(params: APIMember & {
        config: ProjectConfig;
    });
    delete: () => Promise<void>;
}

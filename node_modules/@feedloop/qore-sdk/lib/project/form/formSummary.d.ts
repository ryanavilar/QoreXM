import { ProjectConfig } from "../project";
import { FormMethod } from "./formMethod";
export declare type APIFormSummary = {
    id: string;
    name: string;
    tableId: string;
};
export declare type FormSummary = APIFormSummary & FormMethod;
export declare class FormSummaryImpl implements FormSummary {
    id: string;
    tableId: APIFormSummary["tableId"];
    name: APIFormSummary["name"];
    _config: ProjectConfig;
    constructor(params: APIFormSummary & {
        config: ProjectConfig;
    });
    submit(values: Record<string, string>): Promise<void>;
}

import { ProjectConfig } from "../project";
import { FormMethod } from "./formMethod";
import { APIFormSummary, FormSummaryImpl } from "./formSummary";
export declare type FormFieldSetting = {
    id: string;
    required?: boolean;
    hidden?: boolean;
    defaultValue?: string;
};
export declare type APIForm = APIFormSummary & {
    fields: FormFieldSetting[];
};
export declare type Form = APIForm & FormMethod;
export declare class FormImpl extends FormSummaryImpl implements Form {
    fields: APIForm["fields"];
    constructor(params: APIForm & {
        config: ProjectConfig;
    });
}

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateUrlUserPath = void 0;
function generateUrlUserPath() {
    const url = {
        register() {
            return "/register";
        },
        verify() {
            return "/verify";
        },
        login() {
            return "/login";
        },
        organization(id) {
            return id ? "/orgs/" + id : "/orgs";
        },
        account(orgId, id) {
            return url.organization(orgId) + (id ? "/accounts/" + id : "/accounts");
        },
        project(orgId, id) {
            return url.organization(orgId) + (id ? "/projects/" + id : "/projects");
        }
    };
    return url;
}
exports.generateUrlUserPath = generateUrlUserPath;

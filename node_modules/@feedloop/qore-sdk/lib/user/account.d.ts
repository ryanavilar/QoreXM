import { UrlUserPath } from "./url";
import { APIUser } from "./user";
export declare type APIAccount = {
    id: string;
    user: APIUser;
    type: string;
};
export declare type Account = APIAccount & {
    delete(): Promise<void>;
    updateType(type: string): Promise<void>;
};
export declare class AccountImpl implements Account {
    id: string;
    user: APIUser;
    type: string;
    _orgId: string;
    _url: UrlUserPath;
    _token: string;
    constructor(params: APIAccount & {
        url: UrlUserPath;
        userToken: string;
        orgId: string;
    });
    updateType(type: string): Promise<void>;
    delete(): Promise<void>;
}

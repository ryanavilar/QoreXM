import { UrlUserPath } from "./url";
export declare type APIProject = {
    id: string;
    name: string;
};
export declare type Table = {
    id: string;
    name: string;
};
export declare type Project = APIProject & {
    tables(props?: {
        limit: number;
        offset: number;
    }): Promise<Table[]>;
    delete(): Promise<void>;
    update(project: Partial<APIProject>): Promise<void>;
};
export declare class ProjectImpl implements Project {
    id: string;
    name: string;
    _orgId: string;
    _url: UrlUserPath;
    _token: string;
    constructor(params: APIProject & {
        url: UrlUserPath;
        userToken: string;
        orgId: string;
    });
    get(): Promise<void>;
    tables(props?: {
        limit?: number;
        offset?: number;
    }): Promise<Table[]>;
    delete(): Promise<void>;
    update(project: Partial<APIProject>): Promise<void>;
}
